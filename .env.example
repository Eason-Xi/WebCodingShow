DATABASE_URL="file:./dev.db"
ADMIN_PASSWORD=""
JWT_SECRET=""
# Vercel: replace DATABASE_URL above with a pooled PostgreSQL URL.
# DIRECT_URL: unpooled PostgreSQL URL for migrations (required for PostgreSQL).
DIRECT_URL=""
# Connect a public Vercel Blob store to provision this token.
BLOB_READ_WRITE_TOKEN=""
# Optional: raises GitHub API limits and allows permitted private repositories.
GITHUB_TOKEN=""
